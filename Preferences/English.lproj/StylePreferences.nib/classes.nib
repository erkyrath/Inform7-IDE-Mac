{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = IFPreferencePane; 
            LANGUAGE = ObjC; 
            OUTLETS = {preferenceView = NSView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {styleSetHasChanged = id; }; 
            CLASS = IFStylePreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                changeColours = NSPopUpButton; 
                colourSet = NSPopUpButton; 
                fontSet = NSPopUpButton; 
                fontSize = NSPopUpButton; 
                fontStyle = NSPopUpButton; 
                previewView = NSTextView; 
                tabStopSlider = NSSlider; 
                tabStopView = NSTextView; 
            }; 
            SUPERCLASS = IFPreferencePane; 
        }
    ); 
    IBVersion = 1; 
}