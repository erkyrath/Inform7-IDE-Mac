{
    IBClasses = (
        {
            ACTIONS = {
                compile = id; 
                compileAndDebug = id; 
                compileAndRun = id; 
                continueProcess = id; 
                deleteBreakpoint = id; 
                docIndex = id; 
                findInProject = id; 
                findNext = id; 
                findPrevious = id; 
                newHeaderFile = id; 
                newInformFile = id; 
                newProject = id; 
                openInformFile = id; 
                pauseProcess = id; 
                release = id; 
                renumberSections = id; 
                replayUsingSkein = id; 
                scrollToSelection = id; 
                setBreakpoint = id; 
                shiftLeft = id; 
                shiftRight = id; 
                showFindPanel = id; 
                showInspectors = id; 
                showPreferences = id; 
                stepIntoProcess = id; 
                stepOutProcess = id; 
                stepOverProcess = id; 
                stopProcess = id; 
                useSelectionForFind = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                findInProject = id; 
                newHeaderFile = id; 
                newInformFile = id; 
                showInspectors = id; 
                showPreferences = id; 
            }; 
            CLASS = IFAppDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {extensionsMenu = NSMenuItem; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}