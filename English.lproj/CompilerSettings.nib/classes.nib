{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = IFCompilerOptions; 
            LANGUAGE = ObjC; 
            OUTLETS = {compilerVersion = NSPopUpButton; naturalInform = NSButton; }; 
            SUPERCLASS = IFSetting; 
        }, 
        {
            ACTIONS = {settingsHaveChanged = id; }; 
            CLASS = IFSetting; 
            LANGUAGE = ObjC; 
            OUTLETS = {settingView = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}